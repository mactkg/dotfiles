if [ `uname -s` = "Darwin" ]; then
  EXCLUDES="zsh/colors README.md secrets config/brewfile/Brewfile.lock"
  UNDOTTED="Library local"
else
  EXCLUDES="Library zsh/colors README.md secrets config/brewfile/Brewfile.lock"
  UNDOTTED="local"
fi
